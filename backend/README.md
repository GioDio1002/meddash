# MedDash Backend

FastAPI skeleton for the clinician-assistive MedDash backend.

## Run

```bash
uv run --directory backend backend
```

## Test

```bash
uv run --directory backend pytest
uv run --directory backend ruff check .
```
